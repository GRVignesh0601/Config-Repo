spring:
  application:
    name: KIRA-ORDER-PAYMENT-SERVICE

  data:
    mongodb:
      uri: mongodb://localhost:27017/kira_order_payment_db

server:
  port: 8082

feign:
  circuit-breaker:
    enabled: true

logging:
  level:
    root: INFO
